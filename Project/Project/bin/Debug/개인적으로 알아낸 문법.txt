
--- 섹션 내 key값의 갯수 카운터 ---
   if (fileInfo.Exists) // 파일이 존재할 때.
            {
                var parser = new FileIniDataParser();

                IniData data = parser.ReadFile(TextFilePath);
                foreach (var section in data.Sections)
                {
                    Debug.WriteLine("Section : "+section.SectionName);
                    Debug.WriteLine("Key Count : "+section.Keys.Count);
                }
            }

----